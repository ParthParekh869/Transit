# Winnipeg Transit Open Data API key (server-side only, never sent to the browser)
# Get one at https://api.winnipegtransit.com/
WINNIPEG_TRANSIT_API_KEY=AMr3dweHqDBLZaU5I26i

# Default search radius (meters) when calling /api/transit/stops without &distance=.
# Coordinates are NOT defaulted server-side — the UI prompts the user for their
# location and the AI tool requires lat/lon explicitly.
DEFAULT_RADIUS_M=1000

# AI provider (optional, only needed once you wire up the assistant in /ai/)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
